using GameLogic;
using System;
using System.Collections.Generic;
using System.Linq;

namespace GameUI
{
    static class Program
    {
        public static void Main(string[] args)
        {
            GameSession gs = new GameSession();
            Console.ReadKey();

            //Card[,] board = new Card[8, 8];

            //int sumOfCards = 8 * 8;
            //int pairsOfCards = sumOfCards / 2;


            //for (int rows = 0; rows < 8; rows++)
            //{
            //    for (int columns = 0; columns < 8; columns++)
            //    {
            //        board[rows, columns] = new Card(i);
            //        Console.Write(string.Format("{0, -4}", board[rows, columns].Value));
            //    }
            //    Console.WriteLine();
            //}
        }
    }
}
